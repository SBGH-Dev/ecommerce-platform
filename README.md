# Ecommerce Starter

This is an intentionally implementation-free starter skeleton.

Start with:
1. `docs/ARCHITECTURE.md`
2. `docs/PROJECT_CONTEXT.md`

The Python/TypeScript files contain purpose comments only. They are placeholders
for the real project we will build incrementally.
